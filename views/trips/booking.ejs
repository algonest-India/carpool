<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Booking Confirmation - CarPool Connect</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      padding: 20px;
    }

    .confirmation-card {
      max-width: 600px;
      margin: 0 auto;
      border-radius: 20px;
      overflow: hidden;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      background: white;
    }

    .success-header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 60px 20px;
      text-align: center;
    }

    .success-icon {
      font-size: 80px;
      margin-bottom: 20px;
      animation: scaleIn 0.6s ease-out;
    }

    @keyframes scaleIn {
      0% {
        transform: scale(0);
        opacity: 0;
      }
      50% {
        transform: scale(1.1);
      }
      100% {
        transform: scale(1);
        opacity: 1;
      }
    }

    .confirmation-body {
      padding: 40px 20px;
    }

    .trip-summary {
      background: #f8f9fa;
      border-radius: 15px;
      padding: 20px;
      margin-bottom: 20px;
    }

    .summary-item {
      display: flex;
      align-items: center;
      margin-bottom: 15px;
    }

    .summary-item:last-child {
      margin-bottom: 0;
    }

    .summary-icon {
      width: 40px;
      height: 40px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-right: 15px;
      font-size: 18px;
    }

    .summary-content {
      flex: 1;
    }

    .summary-label {
      font-size: 12px;
      color: #6c757d;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 3px;
    }

    .summary-value {
      font-size: 16px;
      font-weight: 600;
      color: #212529;
    }

    .driver-info {
      display: flex;
      align-items: center;
      background: #f8f9fa;
      border-radius: 15px;
      padding: 20px;
      margin-bottom: 20px;
    }

    .driver-avatar {
      width: 60px;
      height: 60px;
      border-radius: 50%;
      object-fit: cover;
      margin-right: 15px;
      background: #e9ecef;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 30px;
    }

    .driver-details h6 {
      margin: 0;
      font-weight: 600;
      color: #212529;
      margin-bottom: 3px;
    }

    .driver-details small {
      color: #6c757d;
    }

    .booking-reference {
      background: #e7f3ff;
      border-left: 4px solid #0d6efd;
      border-radius: 8px;
      padding: 15px;
      margin-bottom: 20px;
    }

    .booking-reference strong {
      color: #0d6efd;
      display: block;
      margin-bottom: 5px;
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .booking-reference code {
      font-size: 14px;
      font-weight: 600;
      color: #212529;
      font-family: 'Courier New', monospace;
    }

    .action-buttons {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 10px;
    }

    .action-buttons.full {
      grid-template-columns: 1fr;
    }

    .btn-primary-gradient {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border: none;
      color: white;
      font-weight: 600;
      padding: 12px 24px;
      border-radius: 10px;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .btn-primary-gradient:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
      color: white;
      text-decoration: none;
    }

    .btn-secondary-outline {
      background: white;
      border: 2px solid #667eea;
      color: #667eea;
      font-weight: 600;
      padding: 10px 24px;
      border-radius: 10px;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      transition: all 0.2s;
    }

    .btn-secondary-outline:hover {
      background: #667eea;
      color: white;
      text-decoration: none;
    }

    .info-message {
      background: #fff3cd;
      border-left: 4px solid #ffc107;
      border-radius: 8px;
      padding: 15px;
      margin-bottom: 20px;
      color: #856404;
    }

    .info-message i {
      margin-right: 8px;
    }

    .timer {
      text-align: center;
      color: #6c757d;
      font-size: 12px;
      margin-top: 20px;
    }

    .route-info {
      background: #f8f9fa;
      border-radius: 15px;
      padding: 15px;
      margin-bottom: 20px;
    }

    .route-item {
      display: flex;
      align-items: center;
      margin-bottom: 10px;
    }

    .route-item:last-child {
      margin-bottom: 0;
    }

    .route-icon {
      width: 35px;
      height: 35px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-right: 12px;
      font-size: 16px;
    }

    .route-text {
      flex: 1;
    }

    .route-label {
      font-size: 11px;
      color: #6c757d;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 2px;
    }

    .route-value {
      font-size: 14px;
      font-weight: 600;
      color: #212529;
    }
  </style>
</head>
<body>
  <div class="confirmation-card">
    <!-- Success Header -->
    <div class="success-header">
      <div class="success-icon">
        <i class="fas fa-check-circle"></i>
      </div>
      <h1 class="h3 mb-2">Booking Confirmed!</h1>
      <p class="mb-0">Your seat has been successfully booked</p>
    </div>

    <!-- Confirmation Body -->
    <div class="confirmation-body">
      <!-- Booking Reference -->
      <div class="booking-reference">
        <strong>Booking Reference ID</strong>
        <code><%= bookingId %></code>
      </div>

      <!-- Trip Summary -->
      <div class="trip-summary">
        <h6 class="mb-3" style="color: #212529; font-weight: 600;">Trip Details</h6>
        
        <!-- Route Info -->
        <div class="route-info">
          <div class="route-item">
            <div class="route-icon">
              <i class="fas fa-map-marker-alt"></i>
            </div>
            <div class="route-text">
              <div class="route-label">From</div>
              <div class="route-value"><%= trip.origin_text %></div>
            </div>
          </div>
          <div class="route-item">
            <div class="route-icon">
              <i class="fas fa-flag-checkered"></i>
            </div>
            <div class="route-text">
              <div class="route-label">To</div>
              <div class="route-value"><%= trip.destination_text %></div>
            </div>
          </div>
        </div>

        <!-- Summary Items -->
        <div class="summary-item">
          <div class="summary-icon">
            <i class="fas fa-calendar-alt"></i>
          </div>
          <div class="summary-content">
            <div class="summary-label">Departure Date & Time</div>
            <div class="summary-value">
              <%= new Date(trip.departure_timestamp).toLocaleDateString('en-US', { 
                weekday: 'short', 
                month: 'short', 
                day: 'numeric', 
                year: 'numeric' 
              }) %>
              at
              <%= new Date(trip.departure_timestamp).toLocaleTimeString('en-US', { 
                hour: '2-digit', 
                minute: '2-digit',
                hour12: true
              }) %>
            </div>
          </div>
        </div>

        <div class="summary-item">
          <div class="summary-icon">
            <i class="fas fa-users"></i>
          </div>
          <div class="summary-content">
            <div class="summary-label">Your Seat</div>
            <div class="summary-value">1 Seat</div>
          </div>
        </div>

        <div class="summary-item">
          <div class="summary-icon">
            <i class="fas fa-rupee-sign"></i>
          </div>
          <div class="summary-content">
            <div class="summary-label">Price</div>
            <div class="summary-value">
              <% if (trip.price) { %>
                ₹<%= parseFloat(trip.price).toFixed(2) %>
              <% } else { %>
                Free
              <% } %>
            </div>
          </div>
        </div>
      </div>

      <!-- Driver Info -->
      <div class="driver-info">
        <% if (driver && driver.avatar_url) { %>
          <img src="<%= driver.avatar_url %>" alt="<%= driver.full_name %>" class="driver-avatar">
        <% } else { %>
          <div class="driver-avatar" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; font-size: 24px;">
            👤
          </div>
        <% } %>
        <div class="driver-details">
          <h6><%= driver.full_name %></h6>
          <small>Your Driver</small>
        </div>
      </div>

      <!-- Important Info -->
      <div class="info-message">
        <i class="fas fa-info-circle"></i>
        <strong>Important:</strong> Please be at the pickup location 10 minutes before departure time.
      </div>

      <!-- Action Buttons -->
      <div class="action-buttons full">
        <a href="/trips/<%= trip.id %>" class="btn-primary-gradient" style="justify-content: center;">
          <i class="fas fa-arrow-left"></i>
          View Trip Details
        </a>
      </div>

      <div class="action-buttons">
        <a href="/trips" class="btn-secondary-outline" style="justify-content: center;">
          <i class="fas fa-list"></i>
          All Trips
        </a>
        <a href="/profile" class="btn-secondary-outline" style="justify-content: center;">
          <i class="fas fa-user"></i>
          My Bookings
        </a>
      </div>

      <!-- Timer Info -->
      <div class="timer">
        <i class="fas fa-clock"></i>
        Booking confirmed at <%= new Date().toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit' }) %>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
